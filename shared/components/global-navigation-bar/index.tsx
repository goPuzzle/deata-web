import styled from '@emotion/styled';

export const GlobalNavigationBar = () => <Container>GlobalNavigationBar</Container>;

const Container = styled.header`
  position: sticky;
  top: 0;
  left: 0;
  width: 100%;
  background-color: orange;
`;
